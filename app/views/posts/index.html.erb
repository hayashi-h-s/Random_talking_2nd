<% @posts.each do |post| %>
  <div class="card mx-auto" style="width: 90%;">
    <div class="card-body">
      <span class="card-title"><%= post.content %></span>
      <div class="float-right">
        <%#ユーザー機能を付けたら追加する= link_to "編集",folders_path,class: "btn btn-primary" %> 
      </div>
    </div>
  </div>
<% end %>