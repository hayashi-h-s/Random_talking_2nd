<div class="jumbotron">
  <h1 class="display-4">フォルダの作成</h1>
  <p  class="lead">テーマに合わせたフォルダ名をつける。</p>
    <%= form_with model:@folder,local:true,class: "form-inline" do |f| %>
      <%= f.text_field :title,placeholder: "フォルダ名を入力",class: "form-control w-75", autofocus: true %>
      <%= f.submit "投稿",class: "btn btn-primary submit-btn" %>
    <% end %>
</div>

<% 
=begin
%>
<% if @folder.errors.any? %>
  <div class="alert alert-danger">
    <ul>
      <% @folder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

コメントアウトしてる場所

<div class="jumbotron">
  <h2 class="folder-title" ></h2>
  <h1 class="display-4 ">フォルダにトピックを投稿しよう！</h1>
  <p  class="lead">ランダムに表示したいトピックを投稿する。</p>
    <%= form_with model:[@folder, @folder.posts.build],local:true,class: "form-inline" do |f| %>
      <%= f.text_field:content,placeholder: "トピックを入力",class: "form-control w-75" %>
      <%= f.submit "投稿",class: "btn btn-primary submit-btn" %>
    <% end %>
    <div class="text-center">
    <%= link_to "ゲーム開始",folder_path(@folder), class:"btn btn-info btn-lg game-btn" %>
    </div>
</div>


<%
=end
%>
